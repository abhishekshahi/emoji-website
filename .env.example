# Public site URL for canonical links, sitemap, robots.txt, and Open Graph metadata.
# Set this before deploying to production.
NEXT_PUBLIC_SITE_URL=

# Server-only SEO rollout gate (OFF | CANARY | FULL). Default is OFF.
MASTER_SEO_ROLLOUT_MODE=
